function vectorizedIndex = vectorizedCentroids (centroids,nCols,nRows)

vectorizedIndex = nan(size(centroids,1),1);
for iCentroids = 1:length(vectorizedIndex)
    vectorizedIndex(iCentroids) = centroids(iCentroids) * (centroids(iCentroids+size(centroids,1)));
end
